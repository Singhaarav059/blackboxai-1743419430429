<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Results - Plant Disease Detection</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body class="bg-gray-50 min-h-screen">
    <div class="container mx-auto px-4 py-12">
        <div class="text-center mb-8">
            <h1 class="text-3xl font-bold text-green-700 mb-2">Disease Analysis Results</h1>
            <a href="/" class="text-green-600 hover:text-green-800">
                <i class="fas fa-arrow-left mr-1"></i> Back to upload
            </a>
        </div>

        <div class="max-w-4xl mx-auto bg-white rounded-xl shadow-md overflow-hidden">
            <div class="md:flex">
                <div class="md:w-1/2 p-6">
                    <div class="h-64 md:h-full rounded-lg overflow-hidden">
                        <img src="<%= imageURL %>" alt="Uploaded plant image" class="w-full h-full object-cover">
                    </div>
                </div>
                <div class="md:w-1/2 p-6">
                    <div class="mb-6">
                        <h2 class="text-2xl font-bold text-gray-800 mb-2">
                            <%= disease.charAt(0).toUpperCase() + disease.slice(1) %>
                        </h2>
                        <p class="text-sm text-gray-500 italic">
                            Scientific name: <%= diseaseData[disease.replace(/ /g, '_')]?.scientific_name || 'Unknown' %>
                        </p>
                    </div>

                    <div class="mb-6">
                        <h3 class="text-lg font-semibold text-gray-700 mb-2">Recommended Solutions:</h3>
                        <ul class="list-disc pl-5 space-y-2">
                            <% solutions.forEach(solution => { %>
                                <li class="text-gray-700"><%= solution %></li>
                            <% }); %>
                        </ul>
                    </div>

                    <div>
                        <h3 class="text-lg font-semibold text-gray-700 mb-2">Prevention Tips:</h3>
                        <p class="text-gray-700"><%= prevention %></p>
                    </div>

                    <div class="mt-8 pt-6 border-t border-gray-200">
                        <button onclick="window.print()" class="bg-green-600 hover:bg-green-700 text-white font-medium py-2 px-4 rounded-lg transition duration-200">
                            <i class="fas fa-print mr-2"></i> Print Results
                        </button>
                    </div>
                </div>
            </div>
        </div>

        <div class="mt-8 text-center text-sm text-gray-500">
            <p>Note: This is a demonstration system. For accurate diagnosis, consult a plant pathologist.</p>
        </div>
    </div>
</body>
</html>